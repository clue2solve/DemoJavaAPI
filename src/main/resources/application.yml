spring:
  application:
    name: clue2app-java-api-service

  profiles:
    active: default

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    enabled: true
    url: /api-docs
  enable-native-support: true

management:
  endpoint:
    health:
      show-details: always

server:
  port: ${PORT:8080}

clue2app:
  queue:
    name: my-queue-1

logging:
  level:
    root: INFO
    io:
      clue2solve: DEBUG
      clue2app: DEBUG

version: @project.version@